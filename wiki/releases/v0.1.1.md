---
title: v0.1.1
updated: 2026-08-15
status: active
tags: [release]
---

# v0.1.1

## 目的

记录预发布 `v0.1.1`（`0.<era>.<build>`）这一次交付，方便 agent 对照 CHANGELOG 和入口，而不是当正式版。

## 当前判断

这是非正式版。主版本锁在 0。本次只加 build，没有换代。
升到 1.x 等于出正式版，必须用户亲口说「出正式版」。

提交：

- Add Chinese UI, headless VAB MCP, and a Nonos-style wiki. (`5705ad5`)
- Credit Daniel Greenheck in the README and note this fork is by grok-bot. (`b34c946`)
- Add Kerbol patched-conic system and Kerbin–Duna Hohmann transfer. (`9ebaece`)
- Remove node_modules from tracking, add .gitignore (`7017f98`)

## 关键入口

CHANGELOG.md
wiki/releases/v0.1.1.md
wiki/modules/versioning.md
package.json version 字段
scripts/release.mjs

## 边界

不 commit、不 push。不要把本次当成 1.x。不要自行 `--era`。
