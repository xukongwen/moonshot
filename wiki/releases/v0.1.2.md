---
title: v0.1.2
updated: 2026-08-15
status: active
tags: [release]
---

# v0.1.2

## 目的

记录预发布 `v0.1.2`（`0.<era>.<build>`）这一次交付，方便 agent 对照 CHANGELOG 和入口，而不是当正式版。

## 当前判断

这是非正式版。主版本锁在 0。本次只加 build，没有换代。
升到 1.x 等于出正式版，必须用户亲口说「出正式版」。

提交：

- 0.1.N 版本系统（只加 build，拒绝 1.x）
- 整局存档 formatVersion 1（F5/F9，ksp_save / ksp_load）
- 黑话「打板」：wiki + release + 推送

## 关键入口

CHANGELOG.md
wiki/releases/v0.1.2.md
wiki/modules/versioning.md
package.json version 字段
scripts/release.mjs

## 边界

不 commit、不 push。不要把本次当成 1.x。不要自行 `--era`。
