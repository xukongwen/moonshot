---
title: v0.1.3
updated: 2026-08-15
status: active
tags: [release]
---

# v0.1.3

## 目的

记录预发布 `v0.1.3`（`0.<era>.<build>`）这一次交付，方便 agent 对照 CHANGELOG 和入口，而不是当正式版。

## 当前判断

这是非正式版。主版本锁在 0。本次只加 build，没有换代。
升到 1.x 等于出正式版，必须用户亲口说「出正式版」。

提交：

- 交汇对接：多船 `vessels[]`、相对导航、霍曼交汇（80→100 km，2.3 km / 8.3 m/s）
- 对接口 `dock-port-s` + RCS 平移（I/K J/L H/N），硬对接/分离
- 存档 formatVersion 2（仍读 v1）
- 游戏顶栏显示 `v` + package.json 版本

## 关键入口

CHANGELOG.md
wiki/releases/v0.1.3.md
wiki/modules/versioning.md
package.json version 字段
scripts/release.mjs

## 边界

不 commit、不 push。不要把本次当成 1.x。不要自行 `--era`。
